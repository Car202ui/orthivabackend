/**
 * Users, roles and tenants. Mirrors the identity Keycloak asserts (keycloak_user_id) into the domain (person).
 */
@org.springframework.modulith.ApplicationModule(displayName = "identity")
package com.orthiva.core.identity;
